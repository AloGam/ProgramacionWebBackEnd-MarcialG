const { isUtf8 } = require("buffer");
const fs = require("fs");

//generar archivo.txt con contenido
/*
fs.writeFile("archivo.txt", "prueba del archivo de texto", (err)=>{
    if(err)throw err;
    console.log("El archivo se ha creado");
});
*/

//Imprimir el contenido del archivo txt en la terminal
/*
fs.readFile('./archivo.txt', 'Utf8',(err, data) => {
    if (err) throw err;
    console.log(data);
  });
  */

  